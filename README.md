# GORP

A new light reverse proxy in Go.

Here is the [documentation](docs/README.md).

You can define reusable backend pools with the `services` block and reference them from a route using `service: <name>`. The traditional inline `backends` syntax remains supported.

---
